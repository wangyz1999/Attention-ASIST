# Parameters for DVC pipeline

# Parameters specific to the problem to solve
problem: pcvrp
problem_params:
  PRICE_MODE: 1
  DISCOUNT_FACTOR: 0.9
  player_role: engineer  # medic, engineer, maybe searcher?
  medic_model_file: outputs/2021-6-23/graph_size=55,price_mode=1,high_value=1
  medic_graph_size: 55
  rubble_graph_size: 23
  high_value_victim_size: 5
  medic_speed: 0.0091
  engineer_speed: 0.0065
  tool_durability: 20
  distance_ratio: 0.001663
  high_value_mismatch_penalty_coeff: 0.004
  late_rubble_penalty_coeff: 1.757

# Metric file
metric_file: scores/scores.json

# Training parameters
seed: 1234
eval_only: false
no_cuda: false
cuda: 0         # Specify a list of int if want model be distributed across gpus, or a single int otherwise
run_name: run
graph_size: 28
batch_size: 512
n_epochs: 50
epoch_size: 12800
val_size: 10000
val_dataset:
eval_batch_size: 1024
checkpoint_encoder: false
data_distribution: saturn
epoch_start: 0
checkpoint_epochs: 1
log_step: 50
load_path:
resume:
no_tensorboard: false
no_progress_bar: false
no_wandb: false              # Set to true if don't want to log onto WandB
high_value: 1

# Model parameters
model: attention
embedding_dim: 128
hidden_dim: 128
n_encode_layers: 3
tanh_clipping: 10
normalization: batch
lr_model: 1.0e-4
lr_critic: 1.0e-4
lr_decay: 1.0
max_grad_norm: 1.0
exp_beta: 0.8
baseline: rollout
bl_alpha: 0.05
bl_warmup_epochs:
shrink_size:
